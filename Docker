# Use latest code-server image
FROM codercom/code-server:latest

# Set working directory
WORKDIR /home/coder/project

# Expose Render’s dynamic port
EXPOSE 8080

# Start code-server and bind to Render's PORT
CMD ["sh", "-c", "code-server --bind-addr 0.0.0.0:${PORT:-8080} --auth password"]
